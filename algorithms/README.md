# Algorithms

This section holds small algorithm-focused Go examples. The goal is to keep the main idea visible without turning each snippet into a full implementation guide.

- `fisher-yates/`: shuffle a slice with the Fisher-Yates algorithm
