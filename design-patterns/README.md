# Design Patterns

This section holds small Go examples of common design patterns. The intent is to keep the concept understandable without adding extra infrastructure.

- `factory/`: a minimal factory example for transport types
