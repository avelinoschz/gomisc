# Open-Closed

This section contains short examples of the Open/Closed principle in Go. The examples show different ways to extend behavior without rewriting the main logic.

- `composition/`: extending behavior through embedding and composition
- `polymorphism/`: extending behavior through interfaces
