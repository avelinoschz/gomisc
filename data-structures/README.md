# Data Structures

This section contains small Go implementations of common data structures. They are intentionally simple and focused on the core operations.

- `stack/`: LIFO behavior with a slice
- `queue/`: FIFO behavior with a slice
- `linkedlist/`: simple linked nodes with prepend and append
