# Concurrency Patterns

This section contains slightly more practical channel-based patterns. The examples stay small, but they go beyond the most basic primitives.

- `for-select-loop/`: iterating over values sent through a channel
- `done-channel/`: stopping work with a done signal
