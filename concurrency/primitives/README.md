# Concurrency Primitives

This section contains the smallest concurrency building blocks. These examples are meant to show one basic idea at a time.

- `channel-basic/`: unbuffered channel send and receive
- `select-basic/`: waiting on multiple channels with `select`
